#include "queries/spi_camera_generated.h"

#warning "implement IMG_PARAMS (query 0x01) (delete line once done)"
bool deserialize_camera_img_params(camera_img_params_outputs_t *outputs, const uint8_t *buffer, uint16_t buffer_len) {
    // TODO: implement — IMG_PARAMS (query 0x01)
    // this must take buffer and unpack into camera_img_params_outputs_t.
    (void)outputs;
    (void)buffer;
    (void)buffer_len;
    return false;
}